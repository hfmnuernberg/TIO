name: Release

on:
  workflow_dispatch:

jobs:
  semantic-release:
    name: Semantic Release
    uses: hfmnuernberg/TIO/.github/workflows/reusable-semantic-release.yaml@TIO-000_upgrade-pipeline
    secrets: inherit
    with:
      ref: ${{ github.sha }}
