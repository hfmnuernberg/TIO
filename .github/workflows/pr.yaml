name: PR

on:
  pull_request:

jobs:
  verify:
    name: Verify
    uses: hfmnuernberg/TIO/.github/workflows/reusable-verify.yaml@TIO-000_upgrade-pipeline
    secrets: inherit
    with:
      ref: ${{ github.sha }}

  auto-merge:
    name: Auto-merge
    uses: hfmnuernberg/TIO/.github/workflows/reusable-auto-merge.yaml@TIO-000_upgrade-pipeline
    secrets: inherit
    permissions:
      contents: write
      pull-requests: write
    with:
      ref: ${{ github.sha }}
