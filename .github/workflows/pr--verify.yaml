name: PR - Verify

on:
  pull_request:

jobs:
  verify:
    name: Verify
    uses: hfmnuernberg/TIO/.github/workflows/reusable-verify.yaml@TIO-30_run-tests-automatically
    secrets: inherit
    with:
      ref: ${{ github.sha }}
