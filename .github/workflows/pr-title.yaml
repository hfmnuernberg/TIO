name: PR Title

on:
#  pull_request_target:
#    types:
#    - opened
#    - edited
#    - synchronize
#    - reopened
  workflow_dispatch:

jobs:
  verify-pr-title:
    name: Verify PR Title
    uses: hfmnuernberg/TIO/.github/workflows/reusable-verify-pr-title.yaml@main
    with:
      jira_key: TIO
